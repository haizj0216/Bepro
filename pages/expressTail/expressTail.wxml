<header></header>
<body></body>
<view class="head-container">
    <view class="head-express-name">{{expName}}</view>
    <view class="head-basic">订单编号 {{orderId}}</view>
    <view class="head-basic">物流单号 {{expressId}}</view>
</view>
<view class="body-container">
    <view class="timeline-item" wx:for="{{express}}" wx:key="express_{{index}}">
        <view class="time">
            <view class="date-time">{{item.time}}</view>
        </view>
        
        <view class="des">
            <view class="main-status">{{item.status}}</view>
        </view>
    </view>
</view>
